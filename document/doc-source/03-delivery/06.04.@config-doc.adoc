ifndef::imagesdir[:imagesdir: ../images]
:data-uri:

== 文档结构化（zero-doc）

Zero扩展模块新版本开始支持 *文档结构化* 功能，可将一份完整的文档（纸质、电子PDF、其他）结构化存储起来，此模块目前配合集成服务支持的场景如下：

- 针对特殊领域需求实现文档结构化存储（法律、法规、标准、制度），构造完整的文档到库的 *映射* 存储结构。
- 提供文档批注功能，您可直接在文档的某个 *实体部分* 实现 *批注、解读、笔记* 等扩展功能。
- 提供了题目管理、答题功能，此管理功能主要应对如下场景：
+
--
- 调查问卷数据采集功能
- 考试答题功能（线上考试、线下考试、认证考试、模拟考试）
- 企业用于大标准的认证评价模型
--
- 定义了广义的 *引用模型*，您可以在系统中使用各种不同的引用定义，如：`文档 - 章节`，`章节 - 条款`，`条款 - 试卷`，`知识 - 问题` 等关联关系都可以表述。

[CAUTION]
====
上述功能只有在最新版 `1.0-SNAPSHOT` 中才支持的功能，您必须确认您手中的版本已经包含了 `zero-doc` 模块，且最好配合 `zero-is` 使用。
====

include::06.04.01.config-doc-er.adoc[]